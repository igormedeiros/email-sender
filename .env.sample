# Environment mode: 'prod' or 'test'
ENVIRONMENT=test

# SMTP credentials (optional: may also come from config files/secrets manager)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_USE_TLS=true

# Postgres credentials for repositories and tracking (if used)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=
PGDATABASE=treineinsite

# Telegram Bot for operational notifications
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Secret source selection (env, dotenv, aws, azure, vault)
SECRET_SOURCE=env
DOTENV_PATH=.env
